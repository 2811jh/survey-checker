#!/usr/bin/env python
# -*- coding: utf-8 -*-
"""
问卷文件 → 标准格式 转化工具

功能：
  读取任意格式的问卷文件（.md / .txt / .xlsx / .xls），
  提取纯文本内容，输出供 AI Agent 转化使用的结构化上下文，
  并打印标准格式规范，让 Agent 能生成合规的 .standard.md 文件。

用法（由 AI Agent 调用）：
  python convert_to_standard.py <input_file> [--output <output_path>]

输出：
  1. 将原始文件内容以纯文本形式打印到 stdout（供 Agent 读取并转化）
  2. 将标准格式规范打印到 stdout（告知 Agent 输出格式要求）
  3. 如果指定 --output，将 Agent 转化结果写入该路径（由 Agent 在外部执行）

注意：
  本脚本本身不含 AI，转化工作由调用此脚本的 AI Agent 完成。
  脚本只负责：读文件 → 提取内容 → 打印规范。
"""

import argparse
import os
import re
import sys


# ─── 文件读取 ─────────────────────────────────────────────────────────────────

def read_file_content(path: str) -> str:
    """读取文件，返回纯文本内容（自动处理 xlsx/xls/txt/md）"""
    ext = os.path.splitext(path)[1].lower()

    if ext in (".xlsx", ".xls"):
        return _read_excel(path)
    else:
        # txt / md / 其他文本格式
        for encoding in ("utf-8", "gbk", "utf-8-sig"):
            try:
                with open(path, "r", encoding=encoding) as f:
                    return f.read()
            except UnicodeDecodeError:
                continue
        raise ValueError(f"无法以 UTF-8/GBK 编码读取文件：{path}")


def _read_excel(path: str) -> str:
    """将 Excel 文件转为纯文本，逐行合并所有非空单元格"""
    try:
        import openpyxl
    except ImportError:
        raise ImportError("读取 Excel 需要 openpyxl，请执行：pip install openpyxl")

    wb = openpyxl.load_workbook(path, data_only=True)
    lines = []
    for sheet in wb.worksheets:
        if len(wb.worksheets) > 1:
            lines.append(f"\n=== Sheet: {sheet.title} ===")
        for row in sheet.iter_rows(values_only=True):
            cells = [str(c).strip() for c in row if c is not None and str(c).strip()]
            if cells:
                # 同行多列内容用 "  |  " 连接
                lines.append("  |  ".join(cells))
    return "\n".join(lines)


# ─── 标准格式规范 ─────────────────────────────────────────────────────────────

STANDARD_FORMAT_SPEC = """
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋  网易问卷 · 标准上传格式规范 v2.1
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【格式总览】

每道题占一个"块"，块之间用空行分隔。
块的第一行固定格式：  序号[题型]题目标题

序号：从 1 开始连续编号（包含隐含题、分页符）
题型：见下方题型列表

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【支持的题型】

  隐含问题      → [隐含问题]
  描述说明      → [描述说明]
  分页符        → [分页符]
  单选题        → [单选题]
  多选题        → [多选题]
  填空题        → [填空题]
  多项填空题    → [多项填空题]
  量表题        → [量表题]       （横向5/7/10星）
  矩形量表题    → [矩形量表题]   （矩阵×量表，子问题在下方列出）
  矩形单选题    → [矩形单选题]   （矩阵×单选）
  NPS题        → [量表题]       （用 [评分]NPS 标注）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【各题型写法】

▶ 隐含问题
1[隐含问题]uid
[变量类型]1
[变量名称]uid

▶ 描述说明（说明文字直接写在标题里，不换行）
2[描述说明]这里填写说明文字内容

▶ 分页符
3[分页符]null

▶ 单选题（每行一个选项，不加序号/字母）
4[单选题]您的性别是？
男
女
其他/不愿透露

▶ 多选题（选项写法同单选题）
5[多选题]您停止游玩的原因是？（可多选）
内容枯竭/重复
缺乏目标
社交因素
其他

▶ 填空题（标题写完即可，无需额外内容）
6[填空题]（非必填）请描述您的具体建议

▶ 量表题（需注明提示文案和评分档次）
7[量表题]您对游戏的整体满意度评价如何？
[提示文案]非常不满意//一般//非常满意
[评分]5星

  说明：[提示文案] 用 // 分隔三段：左端文案//中间文案//右端文案
  - 三段式：非常不满意//一般//非常满意
  - 两端式（中间为空）：完全不愿意推荐// //绝对愿意推荐
  - NPS：[评分]NPS（自动生成0-10分，提示文案用两端式）

▶ 矩形量表题（子问题每行一个，末尾不加冒号）
8[矩形量表题]请对以下维度进行评分
[提示文案]非常不满意//一般//非常满意
[评分]5星
游戏画面与视觉风格
游戏操作手感与流畅度
游戏内容丰富度

▶ 矩形单选题（选项用 // 分隔，子问题每行一个）
9[矩形单选题]您对以下功能的使用频率如何？
从不//偶尔//有时//经常//总是
联机功能
模组/Mod功能
创造模式

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【跳转逻辑标注（关键！）】

在需要添加跳转逻辑的题目末尾，用 [跳转逻辑] 块描述：

格式：
[跳转逻辑]
当 <选项文字或条件描述> → 显示 <Q编号>
当 <选项文字或条件描述> → 跳转到 <Q编号>
当 <选项文字或条件描述> → 结束问卷

示例（满意度→追问）：
10[量表题]您对游戏的整体满意度？
[提示文案]非常不满意//一般//非常满意
[评分]5星
[跳转逻辑]
当 评分 1-2 分 → 显示 Q11（不满意原因）
当 评分 4-5 分 → 显示 Q12（满意原因）

示例（单选→跳转）：
11[单选题]您是否卸载了游戏？
是的，卸载了
没有，一直保留着
[跳转逻辑]
当 选择"是的，卸载了" → 显示 Q12
当 选择"没有，一直保留着" → 跳转到 Q13

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【文件末尾逻辑块（重要！）】

除了题目级的 [跳转逻辑] 标注外，还必须在文件末尾汇总所有逻辑规则到 [逻辑] 块中。
importer 会读取此块自动写入问卷系统的逻辑规则。

格式：
[逻辑]
源题 Q{n} 选项 {值1},{值2} → 显示 Q{m}
源题 Q{n} 选项 "文本1","文本2" → 显示 Q{m}
源题 Q{n} 子问题 1,2,3 子选项 1,2,3 → 显示 Q{m}

示例：
[逻辑]
源题 Q3 选项 4,5 → 显示 Q4
源题 Q3 选项 1,2 → 显示 Q5
源题 Q8 选项 1,2,3 → 显示 Q9
源题 Q16 选项 "组队开黑" → 显示 Q17,Q18

说明：
- 选项值为数字时表示选项序号（量表题为评分值）
- 选项值用引号包裹时表示选项文本匹配
- 一条规则可显示多个目标题（逗号分隔）
- [逻辑] 块必须放在文件最末尾，所有题目之后

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【特殊选项标注】

在选项行末尾加注释说明特殊属性：

  其他                        # "其他"选项（系统自动开启输入框）
  以上都没有                  # 排他选项（系统自动设为互斥）
  我没有不满意的地方          # 排他选项

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【文件头部规范】

文件第一行写问卷标题（以 [问卷标题] 开头）：
[问卷标题]《我的世界》满意度调研

第二行写问卷说明（以 [问卷说明] 开头，可选）：
[问卷说明]感谢您参与本次调研！您的反馈将帮助我们持续改善游戏体验。

之后从第 1 题开始编号。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【转化规则（AI Agent 须遵守）】

1. 题目编号必须连续，从 1 开始，不可跳号
2. 原文有明确题型标注的（单选/多选/量表等）→ 保留原题型
3. 原文无题型标注的 → 根据内容推断：
     - 选项列表 + "可多选" → 多选题
     - 选项列表（无"可多选"）→ 单选题
     - 开放问答 / "请描述" → 填空题
     - 评分/打分/满意度 + 数值范围 → 量表题
     - 矩阵形式（多子问题×评分/选项）→ 矩形量表题 或 矩形单选题
     - NPS（0-10推荐评分）→ 量表题 + [评分]NPS
4. 选项文字去掉原有的 A/B/C 字母前缀、□/- [ ] 等符号
5. 原文有跳转逻辑说明的 → 必须转写为 [跳转逻辑] 块
6. 原文无明确跳转逻辑但语义上有追问关系的 → 根据上下文推断并添加 [跳转逻辑]
7. 隐含问题、分页符保留原位置
8. 量表题必须有 [提示文案] 和 [评分] 两行
9. 矩形题子问题行末尾不加冒号（自动去除）
10. 填空题如标注"非必填"，在标题里保留"（非必填）"字样
11. * 提示说明文字必须从题干拆出，单独占一行（importer 自动插入 <br> 换行）
12. 如有跳转逻辑，文件末尾必须附加 [逻辑] 块汇总所有逻辑规则

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【常见错误提醒】

❌ 选项和题干写在同一行（应每行一个选项）
❌ 量表题漏写 [提示文案] 或 [评分]
❌ 矩形题子问题末尾带冒号（如"游戏画面："→ 应去掉冒号）
❌ * 提示说明和题干写在同一行（应拆为独立行）
❌ 选项带 A/B/C 字母前缀或 □ 符号（应去掉）
❌ 填空题漏写"（非必填）"
❌ 文件末尾没有 [逻辑] 块（如有跳转逻辑必须汇总）
❌ 编号跳号或从 0 开始（应从 1 连续编号）
❌ NPS 题用 [评分]10星 而非 [评分]NPS
❌ 矩形单选题的选项行缺少 // 分隔符

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
"""


# ─── 主流程 ───────────────────────────────────────────────────────────────────

def main():
    parser = argparse.ArgumentParser(
        description="问卷文件 → 标准格式转化工具（读取原始文件，输出供 AI Agent 转化的上下文）",
        formatter_class=argparse.RawDescriptionHelpFormatter,
    )
    parser.add_argument("input", nargs="?", default=None,
                        help="原始问卷文件路径（支持 .md / .txt / .xlsx / .xls）")
    parser.add_argument("--output", "-o", default=None,
                        help="输出路径（如不指定，默认在原文件同目录生成 <原文件名>.standard.md）")
    parser.add_argument("--spec-only", action="store_true",
                        help="只打印标准格式规范，不读取文件")
    args = parser.parse_args()

    if args.spec_only:
        print(STANDARD_FORMAT_SPEC)
        return

    if not args.input:
        parser.print_help()
        return

    # 确定输出路径
    if args.output:
        output_path = args.output
    else:
        base = os.path.splitext(args.input)[0]
        output_path = base + ".standard.md"

    # 读取文件
    if not os.path.exists(args.input):
        print(f"[ERROR] 文件不存在：{args.input}", file=sys.stderr)
        sys.exit(1)

    print(f"[convert] 读取文件：{args.input}")
    raw_content = read_file_content(args.input)
    filename = os.path.basename(args.input)

    # 输出供 Agent 使用的完整上下文
    print("\n" + "=" * 80)
    print("【原始文件内容】")
    print(f"文件名：{filename}")
    print("=" * 80)
    print(raw_content)
    print("=" * 80)
    print(STANDARD_FORMAT_SPEC)
    print("=" * 80)
    print(f"""
【AI Agent 任务指令】

请将上方"原始文件内容"中的问卷，按照"标准格式规范"完整转化为标准格式。

要求：
1. 严格遵守规范中的所有转化规则（第1-10条）
2. 对每道题的题型给出判断依据（简要注释）
3. 如原文有跳转逻辑，必须完整保留并转写为 [跳转逻辑] 格式
4. 如原文无明确跳转逻辑但上下文有追问关系，请推断并添加
5. 转化完成后，将结果保存到：
   {output_path}
6. 输出完整的标准格式文件内容后，告知用户：
   - 题目总数
   - 有跳转逻辑的题目数量
   - 需要用户确认的推断（题型推断 / 跳转逻辑推断）

现在请开始转化，输出完整的标准格式 Markdown 文件内容。
""")


if __name__ == "__main__":
    main()
